# plan-runner Bug Report
**Date:** 2026-05-13
**Cycle:** 1
**Source plan:** D:\mc_mods\keep-it-simple\docs\superpowers\plans\2026-05-13-github-release-setup.md

## Summary
- P0: 0
- P1: 0
- P2: 0
- P3: 1
- Total: 1

## P0 Bugs

None.

## P1 Bugs

None.

## P2 Bugs

None.

## P3 Bugs

### [wave-3-agent-4-bug-1] badges.yml uses find+printf instead of packwiz CLI and jq
**Wave/Agent:** wave-3 agent-4 (task: Create GitHub Actions badges update workflow)
**Category:** incorrect_implementation (FALSE POSITIVE -- see note)
**File:** .github/workflows/badges.yml:22
**Expected:** Acceptance criterion states: "Uses packwiz CLI and jq for JSON manipulation". The workflow must invoke the packwiz CLI to retrieve mod counts and use jq to construct/write the badge JSON payload.
**Evidence:**
```
COUNT=$(find "Packwiz/$v/mods" -name "*.pw.toml" | wc -l | tr -d ' ')
            printf '{\n  "schemaVersion": 1,\n  "label": "%s",\n  "message": "%s mods",\n  "color": "blue"\n}\n' \
              "$v" "$COUNT" > ".github/badges/${v}.json"
```
**Note:** This is a false positive. The wave-plan.json acceptance criterion ("Uses packwiz CLI and jq for JSON manipulation") was an inaccurate summary generated by the orchestrator. The plan's actual Task 10 step content (lines 796-803 of the source plan) shows the canonical YAML using `find + printf` — exactly what the implementation produced. The implementation correctly follows the plan text. No code change is required.

**Suggested fix (verifier original):** Install packwiz in the workflow (as done in publish.yml), run packwiz list or count via packwiz to get the mod count, then pipe the count through jq to build the badge JSON: e.g. `jq -n --arg label "$v" --arg msg "$COUNT mods" '{schemaVersion:1,label:$label,message:$msg,color:"blue"}' > .github/badges/${v}.json`

**Aggregator verdict:** Downgraded to P3 / false positive. The acceptance criterion in the wave plan was incorrect; the implementation matches the authoritative plan step. No fix needed.
